# Findora AI — Python dependencies
# Install: pip install -r requirements.txt

# ── Computer Vision ───────────────────────────
ultralytics>=8.0.0          # YOLOv8 (auto-downloads yolov8n.pt on first run)
opencv-python>=4.8.0        # Webcam capture & frame processing
numpy>=1.24.0
Pillow>=10.0.0

# ── Backend / API ─────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9     # Form parsing for FastAPI

# ── Data ──────────────────────────────────────
pandas>=2.0.0               # Event log utilities (optional)
